# Content Manager - Analytics Dashboard

백엔드 없이 JSON 파일만으로 작동하는 분석 대시보드입니다.

## 🎯 프로젝트 목표

- **분석 대시보드 구현**: 데이터 시각화와 통계 분석 학습
- **Mock 데이터 활용**: 백엔드 없이 프론트엔드 개발 완성
- **인증 체계 학습**: 토큰 기반 인증의 원리 이해
- **포트폴리오 강화**: 실무 수준의 대시보드 프로젝트

## ✨ 주요 기능

### 1. Mock 인증 시스템
- 로컬 Mock 인증 (가짜 토큰)
- localStorage 기반 세션 관리
- 권한 기반 페이지 접근 제어

**로그인 계정**:
```
관리자:
  email: admin@example.com
  password: admin123

일반 사용자:
  email: user@example.com
  password: user123
```

### 2. 대시보드 (Dashboard)
- 실시간 통계 카드 (페이지뷰, 방문자, 세션, 체류시간)
- 시계열 차트 (일별 트래픽 추이)
- 인기 콘텐츠 순위
- 주간 비교 분석
- 트래픽 소스 분석

### 3. 트래픽 분석 (Traffic)
- 트래픽 통계 카드
- 기간별 트래픽 추이 차트
- 유입 경로 분석 (Direct, Organic, Social 등)
- 디바이스별 접속 비율
- 인기 페이지 순위

### 4. 콘텐츠 현황 (Contents)
- 콘텐츠별 성과 분석 (조회수, 좋아요, 참여율)
- 카테고리별 필터링
- 상태별 필터링 (발행/임시저장)
- 정렬 기능 (조회수, 좋아요순)

### 5. 리포트 (Reports)
- 기간 비교 분석 (이전 기간 대비 변화율)
- 카테고리별 성과 차트
- 트래픽 트렌드 차트
- PDF 리포트 생성 및 미리보기
- CSV 데이터 내보내기

### 6. 설정 (Settings)
- 프로필 관리
- 알림 설정
- 테마 설정

### 7. Mock 데이터 시스템
- JSON 파일 기반 데이터 관리
- 기간 필터에 따른 동적 데이터 변화
- Redux 중앙 상태 관리

## 🏗️ 프로젝트 구조

```
content-manager/
├── public/
│   └── mock-data/              # Mock 데이터 (JSON)
│       ├── auth.json           # 인증 정보
│       ├── traffic.json        # 트래픽 데이터
│       └── contents.json       # 콘텐츠 데이터
│
├── src/
│   ├── modules/
│   │   ├── auth/               # 로그인 페이지
│   │   ├── dashboard/          # 대시보드 (통계, 차트)
│   │   ├── analytics/          # 분석 페이지
│   │   │   ├── TrafficPage     # 트래픽 분석
│   │   │   ├── ContentsPage    # 콘텐츠 현황
│   │   │   └── ReportsPage     # 리포트 (PDF/CSV)
│   │   └── settings/           # 설정 페이지
│   │
│   ├── shared/
│   │   ├── auth/               # Mock 인증 시스템
│   │   ├── store/              # Redux Store (중앙 상태 관리)
│   │   ├── hooks/              # 공통 훅 (PDF, CSV, Alert 등)
│   │   └── components/ui/      # 공통 UI 컴포넌트
│   │
│   └── app/
│       ├── App.tsx
│       ├── layout/             # MainLayout, Sidebar, Header
│       └── router/             # 라우팅 설정
```

## 🚀 시작하기

### 설치

```bash
yarn install
```

### 개발 서버 실행

```bash
yarn dev
```

브라우저에서 `http://localhost:5173` 접속

### 빌드

```bash
yarn build
```

## 📚 기술 스택

### Core
- **React 19** + **TypeScript**
- **Vite** - 빌드 도구
- **Redux Toolkit** - 상태 관리
- **React Router v7** - 라우팅

### UI
- **Tailwind CSS** - 스타일링
- **Recharts** - 차트 라이브러리
- **Lucide React** - 아이콘

### Data Management
- **JSON 파일** - Mock 데이터 소스
- **localStorage** - 클라이언트 저장소

## 🔑 학습 포인트

### 1. Mock 인증 시스템
```typescript
// 로그인 시 가짜 토큰 생성
const mockToken = btoa(JSON.stringify({ userId, timestamp, role }));
localStorage.setItem('authToken', mockToken);
```

### 2. Redux 중앙 상태 관리
```typescript
// 중앙 selector로 모든 페이지에서 일관된 데이터 사용
export const selectDashboardOverview = createSelector(
  [selectTrafficSummary, selectRawTrafficData, selectAnalyticsFilters],
  (summary, rawTrafficData, filters) => {
    // 기간별 동적 데이터 계산
  }
);
```

### 3. ViewModel 패턴
```typescript
// UI와 로직 분리
export function useTrafficViewModel() {
  const overview = useAppSelector(selectDashboardOverview);
  // ...로직 처리
  return { statsCards, timeSeries, isLoading };
}
```

### 4. PDF 리포트 생성
```typescript
// jsPDF를 활용한 PDF 생성
const { generatePdf } = usePdf({ filename: 'analytics_report' });
const blob = await generatePdf({ comparisonData, categoryPerformance });
```

## 📝 개발 완료 현황

### Phase 1: 기반 구축 ✅
- [x] 프로젝트 초기 설정 (Vite + React + TypeScript)
- [x] Mock 데이터 JSON 생성
- [x] Mock 인증 시스템 구현
- [x] Redux Store 설정

### Phase 2: 대시보드 ✅
- [x] 통계 카드 구현 (동적 변화율)
- [x] 차트 연동 (Recharts)
- [x] 인기 콘텐츠 순위
- [x] 주간 비교 분석
- [x] 트래픽 소스 분석

### Phase 3: 분석 페이지 ✅
- [x] 트래픽 분석 페이지
- [x] 콘텐츠 현황 페이지
- [x] 리포트 페이지
- [x] 기간 필터 (7일/30일/90일)
- [x] PDF/CSV 내보내기

### Phase 4: 고급 기능 ✅
- [x] 중앙 상태 관리 (Redux Toolkit)
- [x] ViewModel 패턴 적용
- [x] 코드 스플리팅 (Lazy Loading)
- [x] 에러 바운더리

## 🎤 면접 대응 포인트

### Q: "상태 관리는 어떻게 구현했나요?"

> "Redux Toolkit의 createSelector를 활용해 중앙 집중식 상태 관리를 구현했습니다.
>
> 모든 페이지(대시보드, 트래픽, 리포트)에서 동일한 selector를 재사용하여
> 데이터 일관성을 보장하고, 기간 필터 변경 시 모든 페이지가 동기화됩니다.
>
> 이를 통해 하드코딩된 데이터 없이 동적으로 변화율을 계산합니다."

### Q: "Mock 인증은 어떻게 작동하나요?"

> "로그인 시 사용자 정보를 Base64로 인코딩한 가짜 토큰을 생성해 localStorage에 저장합니다.
>
> ProtectedRoute 컴포넌트에서 토큰 존재 여부를 확인하고,
> 실제 JWT 인증 흐름을 시뮬레이션했습니다.
>
> 이를 통해 토큰 기반 인증의 원리를 학습했습니다."

### Q: "왜 이런 구조로 만들었나요?"

> "ViewModel 패턴을 적용해 UI 컴포넌트와 비즈니스 로직을 분리했습니다.
>
> 각 페이지는 useXxxViewModel 훅을 통해 데이터와 상태를 받아오고,
> 컴포넌트는 순수하게 렌더링만 담당합니다.
>
> 실제 프로젝트에서 백엔드 API로 쉽게 전환할 수 있는 구조로 설계했습니다."

### Q: "PDF 리포트는 어떻게 구현했나요?"

> "jsPDF 라이브러리를 활용해 클라이언트 사이드에서 PDF를 생성합니다.
>
> usePdf 훅으로 재사용 가능하게 추상화했고,
> 미리보기 모달에서 Blob URL로 PDF를 렌더링한 후 다운로드할 수 있습니다."

## 📄 라이선스

MIT License

## 👤 작성자

변세민 (Semin Byun)
- Portfolio: [https://semincode.com](https://semincode.com)
- GitHub: [@zio-s](https://github.com/zio-s)
